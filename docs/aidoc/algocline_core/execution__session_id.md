# algocline-core::execution::session_id

Session identifier type for the `ExecutionService` layer.

## Types

- `SessionId` — Opaque handle that uniquely identifies an execution session across all verb calls.

